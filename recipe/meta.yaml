{% set name = "pyink" %}
{% set version = "26.5.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/pyink-{{ version }}.tar.gz
  sha256: d74d3aa19102069a679160be50cc1340e2fa9d04eece25c24c583e0d85404302

build:
  noarch: python
  entry_points:
    - pyink = pyink:patched_main
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling >=1.20.0
    - hatch-vcs
    - pip
  run:
    - python >={{ python_min }}
    - click >=8.0.0
    - mypy_extensions >=0.4.3
    - packaging >=22.0
    - pathspec >=0.9.0
    - platformdirs >=2
    - tomli >=1.1.0
    - typing-extensions >=4.0.1
    - black =={{ version }}

test:
  imports:
    - pyink
  commands:
    - pip check
    - pyink --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/google/pyink
  summary: Pyink is a python formatter, forked from Black with slightly different behavior.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ngam
